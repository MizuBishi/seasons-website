{
  "name": "seasons-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sass": "sass src/style.scss:style.css --watch",
    "sass:build": "sass src/style.scss:style.css --style compressed"
  },
  "devDependencies": {
    "sass": "^1.87.0"
  }
}